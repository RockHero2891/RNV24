{
  "name": "rnv24-certificacion",
  "version": "1.1.8",
  "private": true,
  "description": "Test RNV24 Certificación - Full Stack Javascript",
  "workspaces": [
    "frontend",
    "backend",
    "shared"
  ],
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,green \"npm run dev -w backend\" \"npm run dev -w frontend\"",
    "build": "npm run build -w shared && npm run build -w frontend && npm run build -w backend",
    "start": "npm run start -w backend",
    "install:all": "npm install",
    "version:update": "node update-version.js",
    "version:check": "node -e \"console.log('Current version:', require('./version.json').version)\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "engines": {
    "node": ">=18"
  }
}
